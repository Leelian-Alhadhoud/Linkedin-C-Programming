#include <stdio.h>

int main()
{
    printf("Here is the value: %d\n" , 27);
    
    // this has two arguments the first is a formatting string 
    /* %d is a placeholder within the formatting string  
    the percent is the placeholder flag and 
    the d represents an integer value in base 10 decimal 
    which in this case it is 27*/

    return (0);
}